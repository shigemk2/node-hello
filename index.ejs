<!DOCTYPE html>
<%
var dt = new Date();
var y = dt.getFullYear(); 
var m = dt.getMonth();
var d = dt.getDay();

%>
<html>
  <head>
    <meta charset="utf-8">
    <title><%=title %></title>
    <link type="text/css" href="./style.css" rel="stylesheet">
  </head>
  <body>
  <h1><%=title %></h1>
  <p><%=msg %></p>
  <p>今日は<%=y + "年" + (m + 1) + "月" + d + "日" %>です</p>
  </body>
</html>
